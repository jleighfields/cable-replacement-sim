---
name: Report
about: Report a bug or request a change
title: ''
labels: ''
---

## What happened

## Expected

## Steps to reproduce
<!-- Include the config that produced it. Every run is reproducible from the
     seed and the config file, so the shortest useful report is the overriding
     values plus simulation.seed — not a description of the run. -->

## Environment
<!-- OS, Python version, Rust toolchain version, package or commit version.
     Say whether the extension was built with `maturin develop` or
     `maturin develop --release`; a debug build is far slower and is the usual
     explanation for a "this is slow" report. -->
